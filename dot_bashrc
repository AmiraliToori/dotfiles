# Enable the subsequent settings only in interactive sessions
case $- in
*i*) ;;
*) return ;;
esac

# ====== Bash Completions ======

# bash-completion
if ! shopt -oq posix; then
  if [ -f /usr/share/bash-completion/bash_completion ]; then
    . /usr/share/bash-completion/bash_completion
  elif [ -f /etc/bash_completion ]; then
    . /etc/bash_completion
  fi
fi

# Wrapper function for yazi file manager
function y() {
  local tmp="$(mktemp -t "yazi-cwd.XXXXXX")"
  yazi "$@" --cwd-file="$tmp"
  if cwd="$(cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
    cd -- "$cwd"
  fi
  rm -f -- "$tmp"
}

# Which plugins would you like to conditionally load? (plugins can be found in ~/.oh-my-bash/plugins/*)
# Custom plugins may be added to ~/.oh-my-bash/custom/plugins/
# Example format:
#  if [ "$DISPLAY" ] || [ "$SSH" ]; then
#      plugins+=(tmux-autoattach)
#  fi

# source "$OSH"/oh-my-bash.sh

# User configuration
# export MANPATH="/usr/local/man:$MANPATH"

# You may need to manually set your language environment
# export LANG=en_US.UTF-8

# Preferred editor for local and remote sessions
# if [[ -n $SSH_CONNECTION ]]; then
#   export EDITOR='vim'
# else
#   export EDITOR='mvim'
# fi

# ====== System Management ======
alias reboot="sudo reboot" # Reboot the system
alias ping="ping -c 5"     # Ping with 5 packets
alias poweroff="sudo systemctl poweroff"

# ====== Listing (ls) Aliases ======
alias l="eza --icons always"
alias ls="eza --icons always"
alias ll="eza -lg --icons always"
alias la="eza -lag --icons always"
alias lt="eza -lTg --icons always"
alias lt1="eza -lTg --level=1 --icons always"
alias lt2="eza -lTg --level=2 --icons always"
alias lt3="eza -lTg --level=3 --icons always"
alias lta="eza -lTag --icons always"
alias lta1="eza -lTag --level=1 --icons always"
alias lta2="eza -lTag --level=2 --icons always"
alias lta3="eza -lTag --level=3 --icons always"

# ======= Grep =======
alias grep="grep --color=auto"

# ======= ip =======
alias ip="ip --color=auto"

# ======= gemini ======
alias gemini="proxychains4 -q gemini"

# ======= agy ========
alias agy="proxychains4 -q agy"

# ======= lazygit ======
alias lzg="lazygit"

# ======= lazydocker ======
alias lzd="lazydocker"

# ======= Code Crafters ========
alias codecrafters="proxychains4 -q codecrafters"

# ====== protontricks-launch =======
alias protontricks='flatpak run com.github.Matoking.protontricks'
alias protontricks-launch='flatpak run --command=protontricks-launch com.github.Matoking.protontricks'

# ====== Shell Integrations ======

# Zoxide
eval "$(zoxide init bash --cmd cd)"

# Tv
eval "$(tv init bash)"

# fzf
eval "$(fzf --bash)"

# Set up fzf key bindings and fuzzy completion
[ -f ~/.fzf.bash ] && source ~/.fzf.bash

# A wrapper Function for exercism
exercism() {
  local out
  readarray -t out < <(command exercism "$@")
  printf '%s\n' "${out[@]}"
  if [[ $1 == "download" && -d "${out[-1]}" ]]; then
    cd "${out[-1]}" || return 1
  fi
}

# A wrapper function for overriding the tests
bats() {
  BATS_RUN_SKIPPED=true command bats *.bats
}

# Flyline - enhanced Bash experience
enable -f $HOME/.local/lib/libflyline.so flyline

if [[ -f ~/.flyline.sh ]]; then
  . ~/.flyline.sh
fi

export PS1='\033[38;2;189;147;249m\u\033[0m@\033[38;2;141;233;263m\h\033[0m:\033[38;2;80;250;123m\w\033[0m\033[38;2;255;121;198m$\033[0m '

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" --no-use # This loads nvm
